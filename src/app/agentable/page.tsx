import { PageClient } from "./page-client";

export const metadata = {
    title: "Agentable | AGI Futures",
    description: "Make software legible to people and AI. Turns websites and apps into a machine-readable UI map.",
};

export default function AgentablePage() {
    return <PageClient />;
}
